# 测试模块化补充验收

本次纠正上一轮“测试集中到 tests 即已完成”的不足：85 个平铺测试按 10 类职责迁移，统一 test_ 命名，提取共享合成样例，统一仓库路径来源。原历史迁移清单不改写，新清单形成后续映射。

- 完整 CPU 回归：570 项通过，182 项子测试通过；保留原 568 项测试并增加 2 项迁移验收。
- 科学测试函数与断言对照迁移前 AST；只规范化仓库根路径表达式。架构测试单独适配新的路径清单。
- 防回退检查：禁止 Paper 1 测试根目录新增测试、禁止通过 import 其他测试模块获取样例，检查新路径没有被 Git 忽略。
- 旧 37 个脚本原字节保持不变；未运行训练、评测、PDF 构建，未发送任何进程信号。

后续用户已确认结束两个旧控制进程，37 个遗留脚本也已完成职责迁移。此阶段的原字节与断言证据不改写；后续导入和路径变化由相邻 runtime-modules 清单继续记录。最终整体验收以运行模块记录及 CI 为准。

恢复依据为 manifest.json 中的 baseline_commit 和 old 路径；从独立 checkout 取回原文件，不覆盖其他人的新修改。
