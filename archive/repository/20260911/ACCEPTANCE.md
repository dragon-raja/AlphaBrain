# 全仓库维护验收（2026-09-11）

## 已实施

- 共享图像工具和配对记录编解码迁入 `AlphaBrain/common`；框架不再反向 import 部署或脚本层。旧路径为兼容别名，无第二份实现。
- 框架包内 7 个测试、验证脚本目录内 2 个测试迁入 `tests`；4 个仍使用的 CABI 工具测试纳入活动测试集。
- CABI 的 193 个历史文件原字节归档，12 个当前调用／身份核查依赖保持原字节。
- DSOL 的 167 个文档／来源／历史报告产物分类；根目录保留导航与两个活动 PDF 兼容链接。后续链接修复仅改变目标，不重写科研结论。
- 2 个报告构建／导出脚本从 docs 移入报告层，并加入显式执行入口。
- 增加仓库级依赖与位置检查、公开测试配置、文档与归档来源链检查、staged 凭据／模型资产检查、pre-commit 配置和 GitHub CI。
- 修复迁移后测试目录、结果说明和已有 PDF 被旧忽略规则误排除的问题；私有模型、原始数组和本地报告仍不上传。

## 验证

- 全量 CPU 回归：568 passed，182 subtests passed。唯一警告为 CPU 环境关闭 CUDA autocast，未启动 GPU。
- Git 可发布文件组成的独立临时副本：便携测试通过；本地当前 PDF 不在副本中，相关检查明确跳过。
- wheel 构建成功，227 个条目；不包含 scripts、docs、reports、archive、tests 或 PHASER 私有实现。
- 使用隔离解释器从 wheel 导入核心共享模块成功。
- 图像处理与记录序列化逐字节对照通过；研究矩阵统计和选择器回归通过。
- 所有既有受保护运行源码、当前 PDF 与标准初态 release 的哈希未变。
- 临时发布副本 staged 内容检查通过；启发式凭据检查不等于完备安全审计。

GitHub CI 的 3.10／3.12 作业需以远端实际结果为准；本机执行环境为 Python 3.12。本记录不虚构远端通过状态。

## 明确边界

Paper 1 根目录的 37 个运行／兼容文件仍保留。它们有原路径、源码身份或活动控制器约束，不能通过改写旧哈希伪装完成迁移。框架已有模型、基准与部署模块保留各自职责，没有为追求少文件而合并或重写算法。

本轮没有启动训练或新 rollout，没有停止常驻控制器，没有更改任务、初态、视角、噪声、成功判据或已发布 PDF。CPU、静态和打包验证不代表完成所有模型／环境的端到端 GPU 与真机认证。

## 恢复和审计

各子目录 manifest 保存旧→新路径与原始、迁移后哈希。`tools/repository/evidence.py` 串联后续变更，不改写旧维护收据。原源码备份可恢复；重复二进制备份与一次未完成迁移的恢复副本仅保留本地，Git 保留原二进制的一份正式归档及其哈希。

当前发布 PDF SHA256：`dbd9c2b6906fee9a120734291dda9dc4b56a16a9194e1852074fb796bfdb95a7`。

当前标准初态 release SHA256：`e300b2c5be13a624518ddb386981fe37eee0bae23294e81446f445db70bf9899`。
