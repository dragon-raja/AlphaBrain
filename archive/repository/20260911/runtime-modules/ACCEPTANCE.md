# 运行模块迁移与旧控制器退出记录

用户明确授权：复核无评测子进程后结束 PID 1957622 和 PID 2346512，保留原源码、实验数据与恢复依据，不启动或重跑实验。

## 已执行的边界

- 通过 pidfd 固定目标，并核对命令行、启动时钟和子进程集合，避免 PID 重用误伤。
- 暂停的旧 full64 控制器没有子进程；使用 SIGKILL 结束，不先恢复它，避免执行实验或清理分支。等待中的旧 PDF 监视器使用 SIGTERM 结束。两者随后均已不存在。
- 没有终止其他任务、修改冻结 release、改写原始评测数据或模型权重。
- 原 37 个脚本全部迁出，不在根目录留转发壳；4 个通用算法／数据模块进入研究包，其他按适配、执行、分析、协议及报告分类。
- 调用方更新为明确模块路径，去掉重复的相对／平铺导入回退。旧冻结 repo 内的命令与身份引用保留历史语义。
- 测试按职责迁移；其断言对照迁移前源码，允许导入与路径更新，不允许删弱科学断言。

## 验收与科学适用范围

验收覆盖：仓库依赖与布局、完整 CPU 测试、四个提取模块的完整非导入 AST、新 CLI 的异地 --help、源码身份路径、原字节备份、发布资产及既有数值回归。

本机结果：576 项测试、182 项子测试通过；便携配置 54 项通过。由暂存 Git 树导出的干净副本：53 项通过、1 项跳过（不随源码发布的当前 PDF），wheel 构建成功。远端 CI 的最终状态以该提交的 Actions 记录为准。

此次没有 GPU rollout、训练或 PDF 重建。新源码布局的首帧／整轨迹等价性尚未用仿真实测认证；后续正式运行必须先做该验收。不能把旧源码哈希重新计算后冒充同一个历史运行版本。

## 恢复

`processes-before.json` 保存原控制进程命令和身份；进程内存状态已结束，不能声称可原地复活。原源码保存在 manifest.json 各条目的 before 路径，带 SHA256；完整旧布局也可从 Git 提交 bcd7a15d6b1e571f04850a09ca1c512beb3f6489 在独立 checkout 恢复。是否恢复旧任务需要单独确认，不能自动重启。

迁移期间当前 PDF 字节未变：`dbd9c2b6906fee9a120734291dda9dc4b56a16a9194e1852074fb796bfdb95a7`。
